# Stage G — Benchmarking and Real-Hardware Experimental Protocol

Status: PENDING — NOT LOCKED

This stage is intentionally not specified yet.

It will define the final experimental methodology for:

- benchmark families and exact sizes;
- exact circuit counts;
- deterministic random seeds;
- timing methodology and repetitions;
- simulator/noise-model policy;
- baseline compiler configurations;
- statistical treatment;
- IBM backend-selection procedure;
- hardware shot counts;
- hardware circuit-selection rules;
- raw-result retention;
- primary and secondary metrics;
- fair-comparison rules;
- resource-budget constraints;
- final research reporting.

No implementation agent should invent these parameters.

The benchmark infrastructure itself is specified in `final-deliverables-spec.md`; this document will later define the exact experimental protocol and may be revised after additional research.
