# This is the same CUDA-Q source represented as plain text for the OQCI frontend.
# Save/copy it to bell_cudaq_input.py if you want to parse it with the CLI.
import cudaq

@cudaq.kernel
def bell():
    q = cudaq.qvector(2)
    h(q[0])
    x.ctrl(q[0], q[1])
    mz(q)
